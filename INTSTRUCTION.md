
---
# 🇷🇺 RU
## • [🇺🇸 EN](#-EN)
# EXCEL → JSON CONVERTER - БЫСТРАЯ ИНСТРУКЦИЯ

---

## 🚀 БЫСТРЫЙ СТАРТ (для Windows)

---

1. Дважды кликните по файлу `excel_to_json_converter.py`
2. Откроется окно программы

### 📋 РУЧНОЙ ЗАПУСК

---

1. Установить зависимости(один раз):

   ```bash
   pip install pandas
   pip install pandas openpyxl
   ```

2. Запустить скрипт:

   ```bash
   python excel_to_json_converter.py
   ```

---

## 🎯 КАК ИСПОЛЬЗОВАТЬ

---

1. Нажмите **"Выбрать Excel"** и выберите один или несколько файлов
2. Нажмите **"Выбрать место для сохранения"** и укажите папку для JSON
3. Выберите режим поиска
4. При необходимости введите ключ, индекс или столбцы
5. Нажмите **"Поиск"** для проверки результатов
6. Нажмите **"Сохранить в JSON"**
7. Готово! JSON файлы будут сохранены в выбранной папке

## ✨ ПОДДЕРЖИВАЕМЫЕ ФУНКЦИИ

---

✓ Поиск по слову с вариациями
✓ Поиск по столбцу или индексу
✓ Поиск по строкам или индексам
✓ По двум столбцам одновременно
✓ Извлечение всех данных
✓ Сохранение результата в JSON

## 🔧 ТРЕБОВАНИЯ

---

* Python 3.7 или выше
* pandas
* openpyxl
* tkinter (входит в стандартную поставку Python)

## ❓ ЧАСТЫЕ ВОПРОСЫ

---

В: Можно ли обрабатывать несколько файлов сразу?
О: Да! Выберите несколько файлов при выборе.

В: Какой формат данных сохраняется в JSON?
О: Все выбранные строки и столбцы сохраняются в стандартный JSON формат.

В: Можно ли искать по двум столбцам сразу?
О: Да, выберите режим поиска по двум столбцам и укажите нужные столбцы.

В: Можно ли удалить файлы из списка перед конвертацией?
О: Да, выберите файлы и нажмите **"Удалить выбранные"**.


## 📧 РЕШЕНИЕ ПРОБЛЕМ

---

Если программа не запускается:

1. Убедитесь, что Python установлен: `python --version`
2. Установите зависимости: `pip install pandas`
3. Установите зависимости: `pip install pandas openpyxl`
4. Проверьте, что файлы Excel существуют и доступны для чтения

---

# 🇺🇸 EN
## • [🇷🇺 RU](#-RU)

# EXCEL → JSON CONVERTER - QUICK START

---

## 🚀 QUICK START (Windows)

---

1. Double-click `excel_to_json_converter.py`
2. Program window will open

## 📋 MANUAL LAUNCH

---

1. Install dependencies (once):

   ```bash
   pip install pandas
   pip install pandas openpyxl
   ```

2. Run the script:

   ```bash
   python excel_to_json_converter.py
   ```

## 🎯 HOW TO USE

---

1. Click **"Select Excel"** and choose one or more files
2. Click **"Select output folder"** and choose a folder for JSON
3. Choose search mode
4. Enter key, index, or columns if needed
5. Click **"Search"** to preview results
6. Click **"Save to JSON"**
7. Done! JSON files will be saved in the selected folder

## ✨ SUPPORTED FEATURES

---

✓ Word search with variations
✓ Search by column or index
✓ Search by rows or indices
✓ Search by two columns
✓ Extract all data
✓ Save results as JSON

## 🔧 REQUIREMENTS

---

* Python 3.7+
* pandas
* openpyxl
* tkinter (included in standard Python)

# ❓ FAQ

---

Q: Can I process multiple files at once?
A: Yes! Select multiple files when choosing.

Q: What JSON format is saved?
A: All selected rows and columns are saved in standard JSON format.

Q: Can I search by two columns at once?
A: Yes, choose the two-column search mode and specify the columns.

Q: Can I remove files from the list before converting?
A: Yes, select the files and click **"Remove selected"**.

## 📧 TROUBLESHOOTING

---

If the program doesn’t start:

1. Check that Python is installed: `python --version`
2. Install dependencies: `pip install pandas`
3. Install dependencies: `pip install pandas openpyxl`
4. Make sure Excel files exist and are readable

---
